import { css } from "@emotion/core";
import { lightTwitchColor } from "../colors";

export const header = css`
  display: flex;
  justify-content: center;
  background-color: ${lightTwitchColor};
  font-size: 10rem;
  color: white;
`;
